// user actions
export const setUser = (user) => ({
    type: "SET_USER",
    user
})

// cohort actions

export const setCohorts = (cohorts) => ({
    type: "SET_COHORTS",
    cohorts
})

// blog actions

export const setUserBlogs = (userBlogs) => ({
    type: "SET_USER_BLOGS",
    userBlogs
})